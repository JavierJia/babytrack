<%= form_for(@out) do |f| %>
  <% if @out.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@out.errors.count, "error") %> prohibited this out from being saved:</h2>

      <ul>
      <% @out.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kind %><br>
    <%= f.text_field :kind %>
  </div>
  <div class="field">
    <%= f.label :descrp %><br>
    <%= f.text_field :descrp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
